diff --git a/dist/MsEdgeTTS.js b/dist/MsEdgeTTS.js
--- a/dist/MsEdgeTTS.js
+++ b/dist/MsEdgeTTS.js
@@ -132,6 +132,9 @@ class MsEdgeTTS {
                 const buffer = index_1.Buffer.from(m.data);
                 const message = buffer.toString();
                 const requestId = /X-RequestId:(.*?)\r\n/gm.exec(message)[1];
+                // Destroyed streams are removed before the WebSocket close handshake finishes.
+                // Ignore their in-flight frames instead of dereferencing deleted request state.
+                if (!Object.hasOwn(this._streams, requestId)) return;
                 if (message.includes(`Path:${messageTypes.TURN_START}`)) {
                     // start of turn, ignore
                     this._log("->", message);
@@ -245,7 +248,7 @@ class MsEdgeTTS {
-    getVoices() {
+    getVoices(signal) {
         return new Promise((resolve, reject) => {
-            axios_1.default.get(MsEdgeTTS.VOICES_URL)
+            axios_1.default.get(MsEdgeTTS.VOICES_URL, { signal })
                 .then((res) => resolve(res.data))
                 .catch(reject);
         });
     }
diff --git a/dist/MsEdgeTTS.d.ts b/dist/MsEdgeTTS.d.ts
--- a/dist/MsEdgeTTS.d.ts
+++ b/dist/MsEdgeTTS.d.ts
@@ -75 +75 @@
-    getVoices(): Promise<Voice[]>;
+    getVoices(signal?: AbortSignal): Promise<Voice[]>;
